#pragma once

// Copy this file to tx_config.h and fill in the isolated experiment network.
// tx_config.h is ignored so credentials are never committed.
#define CSI_TX_WIFI_SSID "replace-me"
#define CSI_TX_WIFI_PASSWORD "replace-me"
